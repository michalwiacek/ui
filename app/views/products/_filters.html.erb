Search Form: <br>
<%= search_form_for q do |f| %>
    <% binding.pry%>
  <%= f.condition_fields do |c| %>
    <%= c.attribute_fields do |a| %>
      <%= a.attribute_select %>                         # < This selects all of the available  
    <% end %>                                           # attributes of your model; makes a dropdown
  <%= c.predicate_select only: [:cont, :lteq, :gteq]%>  # < This is where you can specify  
  <%= c.value_fields do |v| %>                          # what search options you want.
    <%= v.text_field :value %>                          # < This makes the text feild to enter  
  <%end %>                                              # the relevent search query
<%end %>
<%= f.submit %>
<% end %>
<%= link_to "Reset Search", products_path %>