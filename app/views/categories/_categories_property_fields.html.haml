%li.control-group.nested-fields
  .controls
    = f.fields_for :property do |prop|
      = prop.input :name
      = prop.select :field_type, %w[integer string boolean enum decimal]
      = f.hidden_field :_destroy

    = link_to_remove_association "Remove Property", f